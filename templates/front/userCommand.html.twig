{% extends 'front/index.html.twig' %}
{% block main %}
    <main>
        <section class="breadcrumb__area background-bg pt-180 pb-180 include-bg p-relative fix" data-background="assets/img/breadcurmb/breadcurmb.jpg">
            <div class="tp-breadcrmb-circle-one"></div>
            <div class="tp-breadcrmb-circle-two"></div>
            <div class="container">
                <div class="row">
                    <div class="col-12">
                        <div class="breadcrumb__wrapper z-index-6 d-flex justify-content-between align-items-center">
                            <div class="breadcrumb__content">
                                <h3 class="breadcrumb__title">Your Insurance</h3>
                            </div>
                            <div class="breadcrumb__button">
                                <a class="tp-btn-white" href="contact.html">Contact us today<i class="far fa-arrow-right"></i></a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>

<h1>Your Insurances</h1>
    <div class="tp-team-area grey-bg pt-120 pb-90">
    <div class="container">
        <div class="row">
            {% for insurance in insurances %}
                <div class="col-xl-3 col-lg-4 col-md-6">
                    <div class="tp-team-item text-center mb-30">
                    <div class="tp-team-item__thumb">
   
   <img src="{{ asset('uploads/images/' ~ insurance.doacom.insImage) }}" alt="" style="width: 250px; height: 250px;">
</div>
                        <div class="tp-team-item__content">
                            <h4 class="tp-team-title">Insurance: {{ insurance.doacom.nameins }}</h4>
                            <span>Full Premium: {{ insurance.doacom.montant }}</span>
                        </div>
                        <div class="tp-team-item__icon">
                            <a class="team-border-right team-bg-color-1" href="{{ path('reclamationUser_new', {'id': insurance.id}) }}">Add A Claim</i></a>
                            <a class="team-border-right team-bg-color-1" href="{{ path('display_comD', {'idc': insurance.id}) }}">View Details</a>
``

                            <!-- You can add more actions here -->
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
</div>


         </main>
{% endblock %}